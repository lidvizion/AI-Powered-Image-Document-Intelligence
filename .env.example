# Lid Vizion API Configuration
# Leave these empty for simulation mode, or set them for live API calls

# NEXT_PUBLIC_LV_API_URL=https://api.lidvizion.com
# NEXT_PUBLIC_LV_API_KEY=your_api_key_here

# When NEXT_PUBLIC_LV_API_URL is not set, the app runs in simulation mode
# using mock data from /public/mock/response.json
